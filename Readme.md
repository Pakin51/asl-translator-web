เปิด terminal(สำหรับbackend)  
cd .\Backend\  
python -m venv venv  
.\venv\Scripts\activate  
python -m pip install --upgrade pip  
pip install -r requirements.txt  
python mina.py

เปิดอีก terminal(สำหรับfrontend)  
ลงNode.js -> nodejs.org  
cd .\Frontend\asl-frontend\  
npm install  
npm run dev
